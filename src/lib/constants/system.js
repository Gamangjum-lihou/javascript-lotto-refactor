const REWARD = [5_000, 50_000, 1_500_000, 30_000_000, 2_000_000_000];

const RANGE = Object.freeze({
  min: 1,
  max: 45,
});

const PRICE_UNIT = 1000;

const MIN_MATCH_COUNT = 3;

module.exports = {
  RANGE,
  REWARD,
  PRICE_UNIT,
  MIN_MATCH_COUNT,
};
